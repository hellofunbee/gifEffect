<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hello.world.builder.gifeffect"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:screenOrientation="landscape" 
        android:name="hello.world.domain.Mapp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="hello.world.drawpic.DrawPic"
            android:configChanges="layoutDirection|keyboardHidden|screenSize|orientation"
            android:label="@string/app_name"
            android:launchMode="singleTask" >
        </activity>
        <activity android:name="hello.world.act.ShowGifActivity" >
        </activity>
        <activity
            android:name="hello.world.act.SettingActivity"
            android:label="@string/title_activity_setting"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name="hello.world.act.Splash"
            android:launchMode="singleTask" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="hello.world.picgif.PicGif"
            android:launchMode="singleTask" >
        </activity>
    </application>

</manifest>